{
  "name": "time-to-spare",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A calendar for planning a week deliberately — Google Calendar's data, on your own paper.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.11"
  }
}
